dist

# Added by github-init
node_modules/
dist/
build/
.next/
out/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.*.local
coverage/
.nyc_output/
.cache/
.DS_Store
Thumbs.db

# Claude Code
.claude/

# axt
.axt/
.axt-profile.json

# 런타임 로그 산출물 (winston 로테이션 감사 파일 포함)
logs/

# pnpm is the package manager for this repo (pnpm-lock.yaml); do not track other lockfiles
package-lock.json
yarn.lock

# claude code (지침 문서 · 로컬 설정 · 세션 워크트리 · 자동 설치 스킬 · MCP 설정)
CLAUDE.md
.claude.json
.claude.json.*
.claude-*.json

# axt 로컬 프로파일 (백업본 포함)
.axt*

# ast (agent session tracker) 로컬 산출물
.ast/
.ast*
